version: '2'
services:
  postgres:
    image: postgres
    restart: always
    ports:
      - 5432:5432
    volumes:
      - ${DOCKER_DIR}/postgresql:/var/lib/postgresql/data
    environment:
      - POSTGRES_PASSWORD
  mariadb:
    image: mariadb
    restart: always
    ports:
      - 3306:3306
    volumes:
      - ${DOCKER_DIR}/mysql:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
    